Змінна -- іменована частина пам'яті, яка зберігає значення
Логіка -- наука про твердження

a..z, A..Z, 0..9, _ тільки!
_1a (без пробілів, цифри не можуть бути попереду)

variable (змінна/має місце) = literal (літерал/не має місця)
 = -- присвоєння
c = -a -- унарний мінус
c = +a -- унарний плюс
not -- унарний оператор логічної інверсїі, зазначається перед змінною
 > < == != <= >= or (||) and (&&) -- бінарні оператори логіки
 - + -- унарні
 = + - * / ** % // += -= *= /= **= %= //= -- бінарні
 % -- остача від ділення
 // -- ціла частина від ділення
 a = a + b # a += b -- операція з одночасним присвоєнням
 i++ / i--  -- інкремент (постфіксний)
 Масив [] -- структура даних одного типу (класично)
 Матриця -- двовимірний масив
 Функція -- іменований фрагмент коду


--- C++ ---

type name;

b - byte
bool - 1b
char - 1b
short - 2b
int   - 4b
long  - 4b
long long - 8b
float  - 4b (7 digits)
double - 8b (15 digits)
long double - 16b (19 digits)
int* - покажчик(* - pointer) на int -- зберігає значення int змінною
Спеціальний тип данних значенням якого є адреса,
розмір покажчика однаковий для будь-якого типу (залежить від середовища)
*p -- розіменування покажчика
const int* pa = &a  -- покажчик на константне значення
посилання (&) - автоматично розіменований покажчик на константне значення, друге ім'я змінної
int* const pa  -- константний покажчик
const int* const pa = &a -- константний покажчик на константне значення
p + n = p + n * type
*(arr + 1) = arr[i] -- індексатор
int a[300000]; -- статична пам'ять
int* p = new int; -- динамічна пам'ять
p = new int; -- memory leaking
delete p; -- повертання пам'яті
type (*name) (param) -- визначення покажчика на функцію

void hello()
{
	cout << "Hello" << endl;
}
void(*message)() = hello; -- покажчик на функцію
int(*operation[])(int, int) = { add, subtract, multiply, divide };
void (*weapon(int* bullets))(int*)
{
	void(*weapons[])(int*) = { pistol, gun, bow };

	return weapons[findValue(bullets, 3, maxValueArray(bullets, 3))];
}

int a = 5;      int b = 8;    -- посилання (друге ім'я змінної)
int* pa = &a;   int& rb = b;
*pa = 99;       rb = 88;      -- автоматично розіменований покажчик

*(*(p + i) + j) = p[i][j]  -- отримати значення елементу у двовимірному динамічному масиві


cout << var << endl;
cin >> var;

switch (var)
{
    case '':
        break;
}

(condition) ? oper1 : oper2; -- тернарний оператор

for (size_t i = 0; i < length; i++)  -- цикл з параметром
{
    oper1;
    oper2;
}

int i = 1;
while (i <= 10)  -- цикл з (перед)умовою
{
    cout << i << endl;
    i++;
}

do  -- цикл з (пост)умовою
{

} while (cond);

typedef type name; - псевдонім для декількох типів

size_t - elias (псевдонім) - unsigned long long
pow(num, deg) -- степінь
srand(n); -- рандом (зміна константи)
time(0) -- поточний час
rand() % m + i -- рандом
rand() % (maxValue - minValue + 1) + minValue;

type name(param)  -- функція
{
	body;
}

Перевантаження/перегрузка функції (статичний поліморфізм) - різне
виконання функції з однаковим значенням в залежності від параметрів

for (size_t j = 0; j < size-1; j++)  -- сортування бульбашкою
{
	for (size_t i = 0; i < size-1-j; i++)
	{
		if (arr[i] > arr[i + 1])
		{
            swap(arr[i], arr[i + 1]);
			//int a = arr[i];
			//arr[i] = arr[i + 1];
			//arr[i + 1] = a;
		}
	}
}


--- Python ---

abs() -- модуль
sqrt() -- корінь квадратний
a, b = b, a -- кортеж

int = integer (ціле число)
float = fraction number (дробові числа)
str = string (рядок/текст)
bool = boolean = True (1) / False (0), (бінарна логіка)

\t - таб
\n - перенос тексту
end=' '  -- курсор не переноситься

Види алгоритмів:
 лінійний
 умовний (розгалуджений)
 циклічний

if condition:
    oper1
    oper2
elif condition1:
    oper3
    oper4
else:
    oper5
    oper6

pass -- пропустити подальші дії

for i in range(n):  -- цикл з параметром
    oper1
    oper2
while condition:  -- цикл з умовою
    oper1
    oper2

Список значень (list[]) -- набір елементів / об'єднання данних будь-якого типу (в пайтоні)
[[2, 3, 5], [2, 1, 4], [6, 8, 6]] -- список списків (py), матриця (стандарт)

nums = []
nums. - index(), count(), append(), remove(), reverse(), insert(), pop()
min(), max(), sum(), len(), set()
nums[1:4] -- зріз

!! Bubble sort !!
for j in range(len(nums)-1):
    for i in range(len(nums)-1-j):
        if nums[i] > nums[i+1]:
            nums[i], nums[i+1] = nums[i+1], nums[i]

Пошук - лінійний (йде один за одним),
        бінарний (ділить навпіл, працює лише для відсортованого списку)
Сортування - пузирком, вставками, вибором

def name(var (позиційний), *var(ключовий), var=0(за замовченням)):
    func

name(var) - позиційний тип передачі, name(var=0) - ключовий тип

Ім'я (ідентифікатор) - це адреса

.open("назва файлу", "режим відкриття")
  Режими:
w - записати
r - прочитати
a - приєднати